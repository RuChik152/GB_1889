import React from 'react';
import { Input } from './input';

export default {
  title: 'Reactorgam/Input',
  component: Input,
};

const Template = (args) => <Input {...args} />;

export const Primary = Template.bind({});
